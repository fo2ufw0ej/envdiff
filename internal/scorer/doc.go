// Package scorer computes a numeric health score for a set of comparison
// results. The score ranges from 0 (all keys missing or mismatched) to 100
// (all keys identical across every environment).
package scorer
