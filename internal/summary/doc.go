// Package summary aggregates comparator results into high-level statistics.
//
// It provides the Stats type which captures counts of missing, mismatched,
// and identical keys across all compared environments, as well as helper
// methods such as HasDifferences that drive CLI exit-code logic.
package summary
